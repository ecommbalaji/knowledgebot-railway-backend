[build]
dockerfilePath = "services/website_scraping/Dockerfile"
rootDirectory = "."

[deploy]
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 10
healthcheckPath = "/ready"
healthcheckTimeout = 600
# No startCommand - Railway uses CMD from Dockerfile
